{
  "name": "shenzhen-transit-explorer",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:scripts": "node scripts/check-js.mjs",
    "test": "npm run check:scripts && node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
